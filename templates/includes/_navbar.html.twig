<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_main_index') }}">Sensio<span style="color: limegreen;">Events</span></a>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
                <a href="{{ path('app_main_index') }}" class="nav-link" data-turbo-frame="main-section" data-turbo-action="advance">Home</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('app_conference_list') }}" class="nav-link" data-turbo-frame="main-section" data-turbo-action="advance">Conferences</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('app_main_contact') }}" class="nav-link" data-turbo-frame="main-section" data-turbo-action="advance">Contact</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('app_conference_new') }}"
                   data-turbo-frame="modal-frame"
                   data-action="modal#show"
                   class="btn btn-outline-success">New conference</a>
            </li>
        </ul>
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
            {% if app.user %}
                <li class="nav-item">
                    <a href="{{ path('app_logout') }}" class="nav-link" data-turbo="false">Logout</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a href="{{ path('app_login') }}" class="nav-link" data-turbo="false">Login</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_register') }}" class="nav-link" data-turbo="false">Register</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
